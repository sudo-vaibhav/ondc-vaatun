{
  "name": "ondc-vaatun",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"$npm_execpath dev:server\" \"$npm_execpath dev:client\"",
    "d": "$npm_execpath dev",
    "dev:server": "$npm_execpath --filter server dev",
    "dev:client": "$npm_execpath --filter client dev",
    "build": "$npm_execpath run build:client && $npm_execpath run build:server",
    "build:server": "$npm_execpath --filter server build",
    "build:client": "$npm_execpath --filter client build",
    "start": "$npm_execpath --filter server start",
    "lint": "biome check --write",
    "format": "biome format --write",
    "check": "biome check --write",
    "backend": "$npm_execpath --filter server dev",
    "ngrok": "dotenv -e .env -- ngrok http --domain moved-starfish-rapid.ngrok-free.app 4822",
    "n": "$npm_execpath ngrok",
    "signoz:up": "docker compose -f docker-compose.signoz.yml up -d",
    "signoz:down": "docker compose -f docker-compose.signoz.yml down",
    "signoz:clean": "docker compose -f docker-compose.signoz.yml down -v",
    "signoz:logs": "docker compose -f docker-compose.signoz.yml logs -f",
    "test:e2e": "playwright test",
    "e2e": "$npm_execpath test:e2e",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.13",
    "@playwright/test": "^1.57.0",
    "@types/node": "^24",
    "concurrently": "^9.2.1",
    "dotenv-cli": "^11.0.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@opentelemetry/exporter-logs-otlp-http": "^0.212.0",
    "@opentelemetry/instrumentation-pino": "^0.57.0",
    "@opentelemetry/resources": "^2.5.1",
    "@opentelemetry/sdk-logs": "^0.212.0",
    "dotenv": "^17.2.3"
  }
}